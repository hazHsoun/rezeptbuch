import 'package:flutter/material.dart';

class WeeklyPlanSection extends StatelessWidget {
  const WeeklyPlanSection({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('weekly Plan'));
  }
}
